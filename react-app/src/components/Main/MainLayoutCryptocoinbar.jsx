import CompCryptocoinbar from "./CompCryptocoinbar.jsx";

const MainLayoutCryptocoinbar = props => {
  return (
    <section className="main-cryptocoinbar">
      <div className="container-fluid">
        <CompCryptocoinbar />
      </div>
    </section>
  );
};

export default MainLayoutCryptocoinbar;
