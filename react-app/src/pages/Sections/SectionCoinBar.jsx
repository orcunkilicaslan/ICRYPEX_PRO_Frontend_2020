import TopCoinBar from "~/components/Sections/TopCoinBar.jsx";

const SectionCoinBar = props => {
  return (
    <section className="main-cryptocoinbar">
      <div className="container-fluid">
        <TopCoinBar />
      </div>
    </section>
  );
};

export default SectionCoinBar;
